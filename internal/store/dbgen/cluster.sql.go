// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: cluster.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const clearClusterJoin = `-- name: ClearClusterJoin :execrows
DELETE FROM cluster_join WHERE id = 1
`

func (q *Queries) ClearClusterJoin(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, clearClusterJoin)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getClusterJoin = `-- name: GetClusterJoin :one

SELECT id, server_url, token_sealed, updated_at, updated_by FROM cluster_join WHERE id = 1
`

// The join settings are install-wide, so unlike every other query here these
// take no owner_id. See the 00012 migration for why.
func (q *Queries) GetClusterJoin(ctx context.Context) (ClusterJoin, error) {
	row := q.db.QueryRow(ctx, getClusterJoin)
	var i ClusterJoin
	err := row.Scan(
		&i.ID,
		&i.ServerUrl,
		&i.TokenSealed,
		&i.UpdatedAt,
		&i.UpdatedBy,
	)
	return i, err
}

const setClusterJoin = `-- name: SetClusterJoin :one
INSERT INTO cluster_join (id, server_url, token_sealed, updated_by)
VALUES (1, $1, $2, $3)
ON CONFLICT (id) DO UPDATE
    SET server_url   = EXCLUDED.server_url,
        token_sealed = EXCLUDED.token_sealed,
        updated_by   = EXCLUDED.updated_by,
        updated_at   = now()
RETURNING id, server_url, token_sealed, updated_at, updated_by
`

type SetClusterJoinParams struct {
	ServerUrl   string
	TokenSealed []byte
	UpdatedBy   pgtype.UUID
}

func (q *Queries) SetClusterJoin(ctx context.Context, arg SetClusterJoinParams) (ClusterJoin, error) {
	row := q.db.QueryRow(ctx, setClusterJoin, arg.ServerUrl, arg.TokenSealed, arg.UpdatedBy)
	var i ClusterJoin
	err := row.Scan(
		&i.ID,
		&i.ServerUrl,
		&i.TokenSealed,
		&i.UpdatedAt,
		&i.UpdatedBy,
	)
	return i, err
}
